#include<stdio.h>
#include<conio.h>
 int sum(int,int);
 int sub(int,int);
 int multi(int,int);
 int divide(int,int);
 void main()
 {
   clrscr();
   int a,b,s;
   char c;
   printf("enter a and b");
   scanf("%d%d",&a,&b);
   printf("which operation you want to perform(+,-,*,/)\n");
   //scanf("%c",&c);
   c=getche();
   switch(c)
   {
    case '+': printf("the sum of no.are=%d",sum(a,b));
		 break;
    case '-': printf("the sub of no.are=%d",sub(a,b));
		 break;
    case '*': printf("the multi of no.are=%d",multi(a,b));
		 break;
    case '/': printf("the divide of no.are=%d",divide(a,b));
		 break;
  //  default :printf("invalid operation");
   }
   getch();
 }
   int sum(int x,int y)
   {
     return x+y;
   }
   int sub(int x,int y)
   {
     return x-y;
   }
   int multi(int x,int y)
   {
     return x*y;
   }
   int divide(int x,int y)
   {
     return x/y;
   }










